pipeline {
    agent any
    
    tools{
        maven "Maven-3.9.12"
    }

    stages {
        stage('Clone Repo') {
            steps {
                git 'https://github.com/Raj-samunder/maven-web-app.git'
            }
        }
        stage('Maven Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Docker Image') {
            steps {
                sh 'docker build -t rajsamunder/mavenwebapp .'
            }
        }
        stage('k8s deployment') {
            steps {
                sh 'kubectl apply -f k8s-deploy.yml'
            }
        }
    }
}
